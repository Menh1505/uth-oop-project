{
  "name": "notification-service",
  "version": "1.0.0",
  "description": "Simple Notification Service (Email, SMS, Push) with event triggers and mock providers for local testing",
  "main": "dist/server.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc -p .",
    "start": "node dist/server.js",
    "lint": "eslint . --ext .ts",
    "test": "echo \"No tests configured\" && exit 0"
  },
  "keywords": [
    "notification",
    "email",
    "sms",
    "push"
  ],
  "author": "UTH OOP Project",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "glob": "^11.0.3",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.4.2",
    "@types/uuid": "^10.0.0",
    "rimraf": "^6.1.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2"
  }
}
